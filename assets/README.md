# Assets — Landing Musky × Tractive

Solo **imágenes raster** (PNG/JPG) servidas vía `raw.githubusercontent.com` para Instapage.

Los SVG (iconos, líneas decorativas, logos) van **inline** en el HTML generado por el build — no hace falta subirlos aquí.

Fuente del build: `src/tractive-landing/` en el repo web.
